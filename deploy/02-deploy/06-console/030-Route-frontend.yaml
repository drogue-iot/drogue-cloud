kind: Route
apiVersion: route.openshift.io/v1
metadata:
  name: console-frontend
spec:
  to:
    kind: Service
    name: console-frontend
  port:
    targetPort: endpoint
  tls:
    termination: edge
