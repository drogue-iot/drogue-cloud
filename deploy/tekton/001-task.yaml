apiVersion: tekton.dev/v1beta1
kind: Task
metadata:
  name: goodbye
spec:
  steps:
    - name: goodbye
      image: registry.access.redhat.com/ubi8-minimal
      command:
        - echo
      args:
        - "Goodbye world!"
